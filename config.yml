subject: "US Federal Executive Branch — Administrations"

modes:
  biography: false
  rhetoric_action_ledger: true
  cross_subject: true

topic_defaults:
  clusters:
    - executive_orders
    - agency_directives
    - funding_actions
    - enforcement_actions
    - immigration
    - doj
    - dhs
    - cms
    - federalism_conflict
    - conditional_funding
    - retaliation_language
    - judicial_intervention

policies:
  require_reputable_source: true
  allow_secondary_if_cites_primary: true
  ban_memes: true
  require_verbatim_for_statements: true
  append_only_records: true

free_dom:
  enabled: true
  mode: involvement
  relation_fields: [entity, event, date, filing, source_url]

ledger:
  enabled: true
  record_types:
    - statement
    - action
    - link
  required_fields:
    statement: [id, speaker, date_time_utc, verbatim_quote, primary_source_url]
    action: [id, date, actor, policy_instrument, primary_source_url]
    link: [id, statement_id, action_id, relationship_type]

administrations:
  enabled: true
  index_path: "administrations/index.yml"
  allowed_terms:
    - "2017-2021_Trump"
    - "2021-2025_Biden"
    - "2025-2029_Trump"
